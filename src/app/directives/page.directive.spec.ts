import { PageDirective } from './page.directive';

describe('PageDirective', () => {
  it('should create an instance', () => {
    const directive = new PageDirective();
    expect(directive).toBeTruthy();
  });
});
